on: 
  push:
  release:
  workflow_dispatch:
    inputs:
      tags:
        description: 'Test scenario tags'
        required: false 
        type: boolean 

jobs:
  log-the-inputs:
    runs-on: ubuntu-latest
    steps:
      - name: build
        id: build
        env:
          TAGS: ${{ github.event.inputs.tags }}
          IMAGETAG: LATEST
        run: |
          echo "Tags: $TAGS"
          echo ${GITHUB_EVENT_NAME} GITHUB_EVENT_NAME
          echo ${{ github.event_name }}
          echo "::set-output name=image::$IMAGETAG"
      - name: test
        run: |
          echo "{{ steps.build.outputs.image }}"
          echo ${{ steps.build.outputs.image }}